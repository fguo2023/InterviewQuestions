package com.littlebee.binarysearch;

public class BSMain {
    /*
      boundary: [left, right]
      [left, right) left close and right open
      both ways works but depends on the cases that which one we should use.
      1. close interval left = 0, right = len - 1, left = mid - 1 and right = mid + 1
      2. left-closed and right-open interval left = 0, right = len, right = mid, left = mid + 1
      e.g. LeetCode 1011. Capacity To Ship Packages Within D Days, 875. Koko Eating Bananas, 410. Split Array Largest Sum
     */
    public static void main(String[] args) {

    }
}
